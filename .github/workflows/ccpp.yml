name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: env_setup
      run: |
        source env_setup.sh
    - name: make
      run: |
        make -j8
